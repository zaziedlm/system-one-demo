[project]
name = "system-one-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Shuichi Kataoka", email = "shu01k9@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "typesafe-sdk>=0.7.1",
    "python-dotenv>=1.0.1",
]

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"

[tool.uv]
package = false
